defmodule ExdStreams.Query.Builder do
  @moduledoc """
  Helper functions for constructing Exd queries
  """

  @doc """
  Returns a query that streams records from a table stream
  into a meterialised view using the table sink
  """
  # def into_table(destination) do
  #   from s in stream(destination.id),
  #   into: table(destination.id)
  # end

end
